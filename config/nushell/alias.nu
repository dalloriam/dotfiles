# Git
alias gst = git status
alias ga = git add
alias gc = git commit -m

# Navigation
alias sl = ls
alias l = ls
alias s = ls
alias clera = clear
alias celar = clear

# Force the good side of the force
alias vim = nvim

# Tools
alias bazel = bazelisk
